{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "deno",
            "command": "run",
            "args": ["--unstable", "--allow-read", "--allow-write", "--allow-run", "mod.ts"],
            "problemMatcher": ["$deno"],
            "label": "deno: run",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
